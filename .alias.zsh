alias ll='ls -lh'
alias l='ls -Alh'
alias vi='nvim'
alias vim='nvim'

# NOTE make sure to install zoxide with 'sudo pacman -S zoxide'
alias cd='z'

# NOTE make sure to install trash-cli with `sudo pacman -S  trash-cli`
alias rm='trash'
